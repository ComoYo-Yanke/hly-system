<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hly.mapper.UIXMapper">

    <delete id="deleteById">
        delete from user_follow
        <where>
            <if test="followerId != null">follower_id = #{followerId}</if>
            <if test="followingId != null">and following_id = #{followingId}</if>
        </where>
    </delete>

    <select id="queryFollowCountOnlyOne" resultType="java.lang.Integer">
        select count(*) count from user_follow where follower_id = #{id}
    </select>

    <select id="queryFansCountOnlyOne" resultType="java.lang.Integer">
        select count(*) count from user_follow where following_id = #{id}
    </select>
</mapper>